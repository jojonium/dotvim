### Installation:

	git clone https://github.com/jojonium/dotvim.git

### Create symlinks:

	ln -s ~/dotfiles/vimrc ~/.vimrc

### Update submodules:

	cd ~/.vim
	git submodule init
	git submodule update
