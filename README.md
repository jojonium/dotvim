### Installation:

	git clone git://github.com/jojonium/dotvim.git ~/.vim

### Create symlinks:

	ln -s ~/dotfiles/vimrc~/.vimrc

### Update submodules:

	cd ~/.vim
	git submodule init
	git submodule update
